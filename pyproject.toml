[project]
name = "brace-industries"
version = "0.1.0"
description = "A minimal static site generator for personal blogs and project showcases"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "markdown>=3.5.1",
    "jinja2>=3.1.2",
    "pyyaml>=6.0.1",
]
